# Activar motor de reescritura
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Permitir acceso directo a archivos y carpetas existentes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Redirigir todo a index.php
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>

# ============================================
# SEGURIDAD
# ============================================

# Prevenir acceso al archivo .env
<Files .env>
    Order allow,deny
    Deny from all
</Files>

# Prevenir listado de directorios
Options -Indexes

# Bloquear acceso a archivos sensibles
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# ============================================
# CONFIGURACIÓN PHP
# ============================================

# Tamaño máximo de subida de archivos
php_value upload_max_filesize 20M
php_value post_max_size 20M
php_value max_execution_time 300
php_value max_input_time 300

# Configuración de errores
php_flag display_errors On
php_value error_reporting E_ALL

# ============================================
# COMPRESIÓN Y CACHÉ
# ============================================

# Activar compresión Gzip
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

# Caché de archivos estáticos
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>

# ============================================
# PROTECCIÓN CONTRA HOTLINKING (COMENTADO EN DESARROLLO)
# ============================================
# Descomentar en producción y cambiar tudominio.com por tu dominio real
# <IfModule mod_rewrite.c>
#     RewriteCond %{HTTP_REFERER} !^$
#     RewriteCond %{HTTP_REFERER} !^https?://(www\.)?csphuanuco\.org\.pe [NC]
#     RewriteCond %{HTTP_REFERER} !^http(s)?://localhost [NC]
#     RewriteRule \.(jpg|jpeg|png|gif)$ - [NC,F,L]
# </IfModule>